# PLACEHOLDER values only — never commit real secrets. In a real cluster these would be
# created out-of-band (kubectl create secret / sealed-secrets / an external secrets operator),
# not checked into git. stringData keeps them human-readable for this demo.
apiVersion: v1
kind: Secret
metadata:
  name: chargesquare-secrets
  labels:
    app.kubernetes.io/part-of: chargesquare
type: Opaque
stringData:
  POSTGRES_USER: "chargesquare"
  POSTGRES_PASSWORD: "change-me-in-a-real-cluster"
  JWT_SECRET: "change-me-32-byte-minimum-signing-key!!"
  DEMO_ADMIN_PASSWORD: "admin123"
  DEMO_VIEWER_PASSWORD: "viewer123"
